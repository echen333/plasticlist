{
    "private": true,
    "scripts": {
      "start" : "cd backend && uvicorn api.main:app --host 0.0.0.0 --port 8000",
      "dev": "vercel dev",
      "dev:frontend": "cd frontend && npm run dev",
      "dev:backend": "cd backend && uvicorn api.main:app --reload"
    }
  }